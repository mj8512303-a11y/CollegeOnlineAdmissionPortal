<!--@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>-->
@*@ViewBag.Title ?? "Admission Incharge Dashboard - COAP*@<!--"</title>-->
<!-- Google Fonts -->
<!--<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet" />-->
<!-- Bootstrap CSS -->
<!--<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet" />

    <style>
        body {
            margin: 0;
            font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f9fafb;
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* Sidebar */
        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            width: 230px;
            height: 100vh;
            background: linear-gradient(180deg, #007bff, #6610f2);
            color: white;
            overflow-y: auto;
            transition: all 0.3s ease;
            box-shadow: 2px 0 6px rgb(0 0 0 / 0.1);
            z-index: 2000;
        }

        .sidebar .brand {
            font-size: 1.5rem;
            font-weight: 700;
            text-align: center;
            padding: 1.25rem 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.25);
            letter-spacing: 0.05em;
            user-select: none;
        }

        .sidebar ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .sidebar ul li {
            padding: 14px 24px;
        }

        .sidebar ul li a {
            text-decoration: none;
            color: #fff;
            display: flex;
            align-items: center;
            font-weight: 500;
            font-size: 1rem;
            gap: 12px;
            border-radius: 6px;
            transition: background 0.3s ease, padding-left 0.3s ease;
            white-space: nowrap;
            cursor: pointer;
        }

        .sidebar ul li a:hover,
        .sidebar ul li a.active {
            background: rgba(255, 255, 255, 0.25);
            padding-left: 32px;
            font-weight: 600;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
        }

        .sidebar ul li a i {
            font-size: 1.3rem;
            line-height: 1;
        }

        /* Topbar */
        .topbar {
            position: fixed;
            top: 0;
            left: 230px;
            width: calc(100% - 230px);
            height: 60px;
            background-color: #fff;
            box-shadow: 0 2px 8px rgb(0 0 0 / 0.1);
            padding: 0 30px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            z-index: 1500;
        }

        .topbar h5 {
            margin: 0;
            font-weight: 600;
            font-size: 1.2rem;
            color: #333;
            user-select: none;
        }

        .logout-btn {
            color: #dc3545;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .logout-btn:hover {
            text-decoration: underline;
            color: #bd2130;
        }

        /* Main content iframe */
        .main-content {
            position: fixed;
            top: 60px;
            left: 230px;
            width: calc(100% - 230px);
            height: calc(100vh - 60px);
            border: none;
        }

        /* Responsive */
        @@media (max-width: 768px) {
            .sidebar {
                position: relative;
                width: 100%;
                height: auto;
            }

            .topbar {
                left: 0;
                width: 100%;
            }

            .main-content {
                position: relative;
                width: 100%;
                height: auto;
                margin-top: 20px;
                left: 0;
            }

    </style>
</head>

<body>-->
<!-- Sidebar -->
<!--<nav class="sidebar" aria-label="Sidebar navigation">
    <div class="brand">🎓 COAP - Incharge</div>
    <ul>
        <li>
            <a href="@Url.Action("Dashboard", "Incharge")" target="contentFrame" class="active" onclick="setActiveLink(this)">
                <i class="bi bi-speedometer2"></i> Dashboard
            </a>
        </li>
        <li>
            <a href="@Url.Action("ViewApplications", "Incharge")" target="contentFrame" onclick="setActiveLink(this)">
                <i class="bi bi-folder2-open"></i> View Applications
            </a>
        </li>
        <li>
            <a href="@Url.Action("CreateMeritList", "Incharge")" target="contentFrame" onclick="setActiveLink(this)">
                <i class="bi bi-award"></i> Create Merit List
            </a>
        </li>
        <li>
            <a href="@Url.Action("ViewMeritLists", "Incharge")" target="contentFrame" onclick="setActiveLink(this)">
                <i class="bi bi-card-list"></i> View Merit Lists
            </a>
        </li>
        <li>
            <a href="@Url.Action("DownloadMeritList", "Incharge")" target="contentFrame" onclick="setActiveLink(this)">
                <i class="bi bi-download"></i> Download Merit List (PDF)
            </a>
        </li>
        <li>
            <a href="@Url.Action("Logout", "Account")" class="logout-btn" target="_top">
                <i class="bi bi-box-arrow-right"></i> Logout
            </a>
        </li>
    </ul>
</nav>-->
<!-- Topbar -->
<!--<header class="topbar" role="banner">
    <h5>Welcome, @Session["UserName"]</h5>
    <a href="@Url.Action("Logout", "Account")" class="logout-btn d-md-none d-lg-none" target="_top">
        <i class="bi bi-box-arrow-right"></i> Logout
    </a>
</header>-->
<!-- Main Content -->
<!--<iframe src="@Url.Action("Dashboard", "Incharge")" name="contentFrame" class="main-content" title="Main Content"></iframe>
<div>
    @RenderBody();
</div>

<script>
    // Highlight active sidebar link on click
    function setActiveLink(el) {
        const links = document.querySelectorAll('.sidebar ul li a');
        links.forEach(link => link.classList.remove('active'));
        el.classList.add('active');
    }
</script>-->
<!-- Bootstrap JS Bundle and jQuery -->
<!--<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</body>

</html>-->
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <title>Admission Incharge Dashboard - COAP</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet" />

    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f9fafb;
            margin: 0;
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* Sidebar */
        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            width: 250px;
            height: 100vh;
            background: linear-gradient(180deg, #007bff, #6610f2);
            color: #fff;
            padding-top: 1.5rem;
            transition: width 0.3s ease;
            z-index: 1050;
            box-shadow: 2px 0 10px rgb(0 0 0 / 0.15);
        }

        .sidebar .brand {
            font-weight: 700;
            font-size: 1.5rem;
            text-align: center;
            margin-bottom: 1.5rem;
            user-select: none;
        }

        .sidebar ul {
            list-style: none;
            padding-left: 0;
            margin-bottom: 0;
        }

        .sidebar ul li {
            margin-bottom: 0.8rem;
        }

        .sidebar ul li a {
            display: flex;
            align-items: center;
            padding: 12px 25px;
            color: #fff;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            border-radius: 8px;
            transition: background-color 0.3s, padding-left 0.3s;
        }

        .sidebar ul li a:hover,
        .sidebar ul li a.active {
            background-color: rgba(255, 255, 255, 0.2);
            padding-left: 35px;
            text-decoration: none;
        }

        .sidebar ul li a i {
            margin-right: 12px;
            font-size: 1.2rem;
        }

        /* Topbar */
        .topbar {
            position: fixed;
            top: 0;
            left: 250px;
            right: 0;
            height: 60px;
            background-color: #fff;
            border-bottom: 1px solid #dee2e6;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 25px;
            box-shadow: 0 1px 4px rgb(0 0 0 / 0.1);
            z-index: 1040;
        }

        .topbar h5 {
            margin: 0;
            font-weight: 600;
            color: #333;
        }

        .logout-btn {
            color: #dc3545;
            font-weight: 600;
            border: none;
            background: none;
            cursor: pointer;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 6px;
            transition: color 0.3s;
        }

        .logout-btn:hover {
            color: #a71d2a;
            text-decoration: underline;
        }

        /* Main content */
        .main-content {
            margin-left: 250px;
            margin-top: 60px;
            padding: 2rem 2.5rem;
            min-height: calc(100vh - 60px);
            background-color: #f9fafb;
        }

        /* Dashboard header */
        /*.dashboard-header {
            margin-bottom: 1.5rem;
            color: #212529;
            font-weight: 700;
            font-size: 1.75rem;
            user-select: none;
        }*/

        /* Cards grid */
        /*.cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 1.6rem;
        }*/

        /* Individual card */
        /*.card-info {
            background: #fff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 3px 15px rgb(0 0 0 / 0.05);
            cursor: default;
            user-select: none;
            display: flex;
            flex-direction: column;
            transition: box-shadow 0.3s;
        }

        .card-info:hover {
            box-shadow: 0 7px 25px rgb(0 0 0 / 0.12);
        }*/

        /* Card header */
        /*.card-header {
            padding: 0.7rem 1.5rem;
            font-weight: 700;
            font-size: 1.1rem;
            color: #fff;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }*/

        /* Card body */
        /*.card-body {
            padding: 1.5rem 1.5rem 2rem 1.5rem;
            font-weight: 800;
            font-size: 3rem;
            color: #111;
            text-align: center;
            flex-grow: 1;
            display: flex;
            align-items: center;
            justify-content: center;
        }*/

        /* Card colors */
        /*.card-blue .card-header {
            background-color: #0d6efd;
        }

        .card-green .card-header {
            background-color: #198754;
        }

        .card-yellow .card-header {
            background-color: #ffc107;
            color: #212529;
        }

        .card-red .card-header {
            background-color: #dc3545;
        }

        .card-gray .card-header {
            background-color: #6c757d;
        }*/

        /* Responsive */
        @@media (max-width: 768px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
                padding-bottom: 0.5rem;
                box-shadow: none;
            }

            .topbar {
                left: 0;
                width: 100%;
            }

            .main-content {
                margin-left: 0;
                margin-top: 110px;
                padding: 1rem 1.2rem;
            }
        }
        
    </style>
</head>

<body>
    <!-- Sidebar -->
    <nav class="sidebar">
        <div class="brand">🎓 COAP - Incharge</div>
        <ul>
            <li><a href="@Url.Action("Dashboard", "Incharge")" class="active"><i class="bi bi-speedometer2"></i> Dashboard</a></li>
            <li><a href="@Url.Action("ViewApplications", "Incharge")"><i class="bi bi-folder2-open"></i> View Applications</a></li>
            <li><a href="@Url.Action("CreateMeritList", "Incharge")"><i class="bi bi-award"></i> Create Merit List</a></li>
            <li><a href="@Url.Action("ViewMeritLists", "Incharge")"><i class="bi bi-card-list"></i> View Merit Lists</a></li>
            <li><a href="@Url.Action("DownloadMeritList", "Incharge")"><i class="bi bi-download"></i> Download Merit List (PDF)</a></li>
            <li><a href="@Url.Action("Logout", "Account")" class="text-danger logout-btn"><i class="bi bi-box-arrow-right"></i> Logout</a></li>
        </ul>
    </nav>

    <!-- Topbar -->
    <header class="topbar">
        <h5>Welcome, @Session["UserName"]</h5>
        @*<form action="@Url.Action("Logout", "Account")" method="post" style="margin:0;">
            <button type="submit" class="logout-btn" title="Logout">
                <i class="bi bi-box-arrow-right"></i> Logout
            </button>
        </form>*@
    </header>

    <!-- Main content -->
    <!--<main class="main-content">
        <div class="dashboard-header">Dashboard</div>

        <div class="cards-grid">
            <div class="card-info card-blue">
                <div class="card-header">Total Applicants</div>
                <div class="card-body">@ViewBag.TotalApplicants</div>
            </div>
            <div class="card-info card-green">
                <div class="card-header">Approved Applications</div>
                <div class="card-body">@ViewBag.Approved</div>
            </div>
            <div class="card-info card-yellow">
                <div class="card-header">Pending Applications</div>
                <div class="card-body">@ViewBag.Pending</div>
            </div>
            <div class="card-info card-red">
                <div class="card-header">Rejected Applications</div>
                <div class="card-body">@ViewBag.Rejected</div>
            </div>
            <div class="card-info card-gray">
                <div class="card-header">Programs</div>
                <div class="card-body">@ViewBag.Programs</div>
            </div>
            <div class="card-info card-blue">
                <div class="card-header">Incharges</div>
                <div class="card-body">@ViewBag.Incharges</div>
            </div>
        </div>
    </main>-->

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>